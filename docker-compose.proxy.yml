name: benchmarking-proxy-dev
services:
  nginx:
    image: nginx:alpine
    container_name: benchmarking-nginx-dev
    volumes:
      - ./ops/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ops/nginx/.htpasswd:/etc/nginx/.htpasswd:ro
      - ./ops/nginx/.basic-auth-hosts.map:/etc/nginx/basic-auth-hosts.map:ro
      - ./maintenance:/usr/share/nginx/html/maintenance:ro
    ports:
      - "3010:3010"
    networks:
      benchmarking-public:
        aliases:
          - docker
    restart: unless-stopped

networks:
  benchmarking-public:
    external: true
